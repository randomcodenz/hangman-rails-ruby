<%= render guess_attempt flash[:guess_attempt] %>

<%= render guess_result flash[:guess_correct], flash[:guess_attempt] %>

<%= render incorrect_guesses @game.incorrect_guesses %>

<%= render game_over @game %>

<%= render game_in_progress @game %>
